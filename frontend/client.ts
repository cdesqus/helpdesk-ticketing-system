// Code generated by the Encore 1.50.4 client generator. DO NOT EDIT.

// Disable eslint, jshint, and jslint for this file.
/* eslint-disable */
/* jshint ignore:start */
/*jslint-disable*/
import type { CookieWithOptions } from "encore.dev/api";

/**
 * BaseURL is the base URL for calling the Encore application's API.
 */
export type BaseURL = string

export const Local: BaseURL = "http://localhost:4000"

/**
 * Environment returns a BaseURL for calling the cloud environment with the given name.
 */
export function Environment(name: string): BaseURL {
    return `https://${name}-.encr.app`
}

/**
 * PreviewEnv returns a BaseURL for calling the preview environment with the given PR number.
 */
export function PreviewEnv(pr: number | string): BaseURL {
    return Environment(`pr${pr}`)
}

const BROWSER = typeof globalThis === "object" && ("window" in globalThis);

/**
 * Client is an API client for the  Encore application.
 */
export class Client {
    public readonly asset: asset.ServiceClient
    public readonly auth: auth.ServiceClient
    public readonly ticket: ticket.ServiceClient
    private readonly options: ClientOptions
    private readonly target: string


    /**
     * Creates a Client for calling the public and authenticated APIs of your Encore application.
     *
     * @param target  The target which the client should be configured to use. See Local and Environment for options.
     * @param options Options for the client
     */
    constructor(target: BaseURL, options?: ClientOptions) {
        this.target = target
        this.options = options ?? {}
        const base = new BaseClient(this.target, this.options)
        this.asset = new asset.ServiceClient(base)
        this.auth = new auth.ServiceClient(base)
        this.ticket = new ticket.ServiceClient(base)
    }

    /**
     * Creates a new Encore client with the given client options set.
     *
     * @param options Client options to set. They are merged with existing options.
     **/
    public with(options: ClientOptions): Client {
        return new Client(this.target, {
            ...this.options,
            ...options,
        })
    }
}

/**
 * Import the auth handler to be able to derive the auth type
 */
import type { auth as auth_auth } from "~backend/auth/auth";

/**
 * ClientOptions allows you to override any default behaviour within the generated Encore client.
 */
export interface ClientOptions {
    /**
     * By default the client will use the inbuilt fetch function for making the API requests.
     * however you can override it with your own implementation here if you want to run custom
     * code on each API request made or response received.
     */
    fetcher?: Fetcher

    /** Default RequestInit to be used for the client */
    requestInit?: Omit<RequestInit, "headers"> & { headers?: Record<string, string> }

    /**
     * Allows you to set the authentication data to be used for each
     * request either by passing in a static object or by passing in
     * a function which returns a new object for each request.
     */
    auth?: RequestType<typeof auth_auth> | AuthDataGenerator
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import {
    listAudits as api_asset_audit_listAudits,
    scanAsset as api_asset_audit_scanAsset
} from "~backend/asset/audit";
import {
    bulkImportAssets as api_asset_bulk_import_bulkImportAssets,
    generateImportTemplate as api_asset_bulk_import_generateImportTemplate
} from "~backend/asset/bulk-import";
import { createAsset as api_asset_create_createAsset } from "~backend/asset/create";
import { deleteAsset as api_asset_delete_deleteAsset } from "~backend/asset/delete";
import {
    exportAssets as api_asset_export_exportAssets,
    exportAuditReport as api_asset_export_exportAuditReport
} from "~backend/asset/export";
import { getAsset as api_asset_get_getAsset } from "~backend/asset/get";
import { listAssets as api_asset_list_listAssets } from "~backend/asset/list";
import {
    generateQRCode as api_asset_qr_code_generateQRCode,
    generateQRLabel as api_asset_qr_code_generateQRLabel
} from "~backend/asset/qr-code";
import { getAssetStats as api_asset_stats_getAssetStats } from "~backend/asset/stats";
import {
    adjustStock as api_asset_stock_adjustStock,
    getLowStockItems as api_asset_stock_getLowStockItems,
    getStockTransactions as api_asset_stock_getStockTransactions
} from "~backend/asset/stock";
import { updateAsset as api_asset_update_updateAsset } from "~backend/asset/update";

export namespace asset {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.adjustStock = this.adjustStock.bind(this)
            this.bulkImportAssets = this.bulkImportAssets.bind(this)
            this.createAsset = this.createAsset.bind(this)
            this.deleteAsset = this.deleteAsset.bind(this)
            this.exportAssets = this.exportAssets.bind(this)
            this.exportAuditReport = this.exportAuditReport.bind(this)
            this.generateImportTemplate = this.generateImportTemplate.bind(this)
            this.generateQRCode = this.generateQRCode.bind(this)
            this.generateQRLabel = this.generateQRLabel.bind(this)
            this.getAsset = this.getAsset.bind(this)
            this.getAssetStats = this.getAssetStats.bind(this)
            this.getLowStockItems = this.getLowStockItems.bind(this)
            this.getStockTransactions = this.getStockTransactions.bind(this)
            this.listAssets = this.listAssets.bind(this)
            this.listAudits = this.listAudits.bind(this)
            this.scanAsset = this.scanAsset.bind(this)
            this.updateAsset = this.updateAsset.bind(this)
        }

        public async adjustStock(params: RequestType<typeof api_asset_stock_adjustStock>): Promise<ResponseType<typeof api_asset_stock_adjustStock>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                quantity:        params.quantity,
                reason:          params.reason,
                referenceNumber: params.referenceNumber,
                transactionType: params.transactionType,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/assets/${encodeURIComponent(params.assetId)}/stock/adjust`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_asset_stock_adjustStock>
        }

        /**
         * Bulk import assets from Excel file.
         */
        public async bulkImportAssets(params: RequestType<typeof api_asset_bulk_import_bulkImportAssets>): Promise<ResponseType<typeof api_asset_bulk_import_bulkImportAssets>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/assets/bulk-import`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_asset_bulk_import_bulkImportAssets>
        }

        /**
         * Creates a new asset.
         */
        public async createAsset(params: RequestType<typeof api_asset_create_createAsset>): Promise<ResponseType<typeof api_asset_create_createAsset>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/assets`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_asset_create_createAsset>
        }

        /**
         * Deletes an asset (admin only).
         */
        public async deleteAsset(params: { id: number }): Promise<void> {
            await this.baseClient.callTypedAPI(`/assets/${encodeURIComponent(params.id)}`, {method: "DELETE", body: undefined})
        }

        /**
         * Exports assets to Excel or PDF format.
         */
        public async exportAssets(params: RequestType<typeof api_asset_export_exportAssets>): Promise<ResponseType<typeof api_asset_export_exportAssets>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                assignedUser: params.assignedUser,
                category:     params.category === undefined ? undefined : String(params.category),
                format:       String(params.format),
                search:       params.search,
                status:       params.status === undefined ? undefined : String(params.status),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/assets/export`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_asset_export_exportAssets>
        }

        /**
         * Exports audit report to Excel or PDF format.
         */
        public async exportAuditReport(params: RequestType<typeof api_asset_export_exportAuditReport>): Promise<ResponseType<typeof api_asset_export_exportAuditReport>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                endDate:   params.endDate,
                format:    String(params.format),
                startDate: params.startDate,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/assets/audit-report`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_asset_export_exportAuditReport>
        }

        /**
         * Generate Excel template for bulk import
         */
        public async generateImportTemplate(): Promise<ResponseType<typeof api_asset_bulk_import_generateImportTemplate>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/assets/import-template`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_asset_bulk_import_generateImportTemplate>
        }

        public async generateQRCode(params: { id: number }): Promise<ResponseType<typeof api_asset_qr_code_generateQRCode>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/assets/${encodeURIComponent(params.id)}/qr-code`, {method: "POST", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_asset_qr_code_generateQRCode>
        }

        public async generateQRLabel(params: RequestType<typeof api_asset_qr_code_generateQRLabel>): Promise<ResponseType<typeof api_asset_qr_code_generateQRLabel>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                labelSize: params.labelSize,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/assets/${encodeURIComponent(params.id)}/qr-label`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_asset_qr_code_generateQRLabel>
        }

        /**
         * Retrieves a specific asset by ID.
         */
        public async getAsset(params: { id: number }): Promise<ResponseType<typeof api_asset_get_getAsset>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/assets/${encodeURIComponent(params.id)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_asset_get_getAsset>
        }

        /**
         * Retrieves asset statistics with role-based filtering.
         */
        public async getAssetStats(): Promise<ResponseType<typeof api_asset_stats_getAssetStats>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/assets/stats`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_asset_stats_getAssetStats>
        }

        public async getLowStockItems(): Promise<ResponseType<typeof api_asset_stock_getLowStockItems>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/assets/stock/low`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_asset_stock_getLowStockItems>
        }

        public async getStockTransactions(params: { assetId: number }): Promise<ResponseType<typeof api_asset_stock_getStockTransactions>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/assets/${encodeURIComponent(params.assetId)}/stock/transactions`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_asset_stock_getStockTransactions>
        }

        /**
         * Retrieves assets with filtering and pagination.
         */
        public async listAssets(params: RequestType<typeof api_asset_list_listAssets>): Promise<ResponseType<typeof api_asset_list_listAssets>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                assignedUser: params.assignedUser,
                category:     params.category === undefined ? undefined : String(params.category),
                limit:        params.limit === undefined ? undefined : String(params.limit),
                offset:       params.offset === undefined ? undefined : String(params.offset),
                search:       params.search,
                sortField:    params.sortField === undefined ? undefined : String(params.sortField),
                sortOrder:    params.sortOrder === undefined ? undefined : String(params.sortOrder),
                status:       params.status === undefined ? undefined : String(params.status),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/assets`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_asset_list_listAssets>
        }

        public async listAudits(params: RequestType<typeof api_asset_audit_listAudits>): Promise<ResponseType<typeof api_asset_audit_listAudits>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                assetId: params.assetId === undefined ? undefined : String(params.assetId),
                limit:   params.limit === undefined ? undefined : String(params.limit),
                offset:  params.offset === undefined ? undefined : String(params.offset),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/assets/audits`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_asset_audit_listAudits>
        }

        public async scanAsset(params: RequestType<typeof api_asset_audit_scanAsset>): Promise<ResponseType<typeof api_asset_audit_scanAsset>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/assets/scan`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_asset_audit_scanAsset>
        }

        /**
         * Updates an existing asset.
         */
        public async updateAsset(params: RequestType<typeof api_asset_update_updateAsset>): Promise<ResponseType<typeof api_asset_update_updateAsset>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                assetId:            params.assetId,
                assignedUser:       params.assignedUser,
                assignedUserEmail:  params.assignedUserEmail,
                brandName:          params.brandName,
                category:           params.category,
                comments:           params.comments,
                dateAcquired:       params.dateAcquired,
                hostname:           params.hostname,
                isConsumable:       params.isConsumable,
                location:           params.location,
                minStockLevel:      params.minStockLevel,
                model:              params.model,
                productName:        params.productName,
                quantity:           params.quantity,
                serialNumber:       params.serialNumber,
                status:             params.status,
                totalLicenses:      params.totalLicenses,
                usedLicenses:       params.usedLicenses,
                warrantyExpiryDate: params.warrantyExpiryDate,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/assets/${encodeURIComponent(params.id)}`, {method: "PUT", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_asset_update_updateAsset>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import {
    getActiveSessions as api_auth_login_getActiveSessions,
    login as api_auth_login_login,
    refreshSession as api_auth_login_refreshSession
} from "~backend/auth/login";
import { logout as api_auth_logout_logout } from "~backend/auth/logout";
import {
    forgotPassword as api_auth_password_reset_forgotPassword,
    resetPassword as api_auth_password_reset_resetPassword
} from "~backend/auth/password-reset";
import {
    changePassword as api_auth_users_changePassword,
    createUser as api_auth_users_createUser,
    deleteUser as api_auth_users_deleteUser,
    getCurrentUser as api_auth_users_getCurrentUser,
    listUsers as api_auth_users_listUsers,
    updateUser as api_auth_users_updateUser
} from "~backend/auth/users";

export namespace auth {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.changePassword = this.changePassword.bind(this)
            this.createUser = this.createUser.bind(this)
            this.deleteUser = this.deleteUser.bind(this)
            this.forgotPassword = this.forgotPassword.bind(this)
            this.getActiveSessions = this.getActiveSessions.bind(this)
            this.getCurrentUser = this.getCurrentUser.bind(this)
            this.listUsers = this.listUsers.bind(this)
            this.login = this.login.bind(this)
            this.logout = this.logout.bind(this)
            this.refreshSession = this.refreshSession.bind(this)
            this.resetPassword = this.resetPassword.bind(this)
            this.updateUser = this.updateUser.bind(this)
        }

        /**
         * Changes a user's password (admin only).
         */
        public async changePassword(params: RequestType<typeof api_auth_users_changePassword>): Promise<void> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                newPassword: params.newPassword,
            }

            await this.baseClient.callTypedAPI(`/auth/users/${encodeURIComponent(params.id)}/password`, {method: "POST", body: JSON.stringify(body)})
        }

        /**
         * Creates a new user (admin only).
         */
        public async createUser(params: RequestType<typeof api_auth_users_createUser>): Promise<ResponseType<typeof api_auth_users_createUser>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/auth/users`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_auth_users_createUser>
        }

        /**
         * Deletes a user (admin only).
         */
        public async deleteUser(params: { id: number }): Promise<void> {
            await this.baseClient.callTypedAPI(`/auth/users/${encodeURIComponent(params.id)}`, {method: "DELETE", body: undefined})
        }

        /**
         * Initiates password reset process.
         */
        public async forgotPassword(params: RequestType<typeof api_auth_password_reset_forgotPassword>): Promise<ResponseType<typeof api_auth_password_reset_forgotPassword>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/auth/forgot-password`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_auth_password_reset_forgotPassword>
        }

        /**
         * Debug endpoint to check active sessions (admin only)
         */
        public async getActiveSessions(): Promise<ResponseType<typeof api_auth_login_getActiveSessions>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/auth/sessions`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_auth_login_getActiveSessions>
        }

        /**
         * Gets current user info.
         */
        public async getCurrentUser(): Promise<ResponseType<typeof api_auth_users_getCurrentUser>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/auth/me`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_auth_users_getCurrentUser>
        }

        /**
         * Lists all users (admin only).
         */
        public async listUsers(): Promise<ResponseType<typeof api_auth_users_listUsers>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/auth/users`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_auth_users_listUsers>
        }

        /**
         * Authenticates a user with username and password.
         */
        public async login(params: RequestType<typeof api_auth_login_login>): Promise<ResponseType<typeof api_auth_login_login>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/auth/login`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_auth_login_login>
        }

        /**
         * Logs out a user by invalidating their session.
         */
        public async logout(params: RequestType<typeof api_auth_logout_logout>): Promise<ResponseType<typeof api_auth_logout_logout>> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                token: params.token,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/auth/logout`, {headers, method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_auth_logout_logout>
        }

        /**
         * Endpoint to extend session (refresh session expiration)
         */
        public async refreshSession(): Promise<ResponseType<typeof api_auth_login_refreshSession>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/auth/refresh`, {method: "POST", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_auth_login_refreshSession>
        }

        /**
         * Resets password using token.
         */
        public async resetPassword(params: RequestType<typeof api_auth_password_reset_resetPassword>): Promise<ResponseType<typeof api_auth_password_reset_resetPassword>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/auth/reset-password`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_auth_password_reset_resetPassword>
        }

        /**
         * Updates a user (admin only).
         */
        public async updateUser(params: RequestType<typeof api_auth_users_updateUser>): Promise<ResponseType<typeof api_auth_users_updateUser>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                email:    params.email,
                fullName: params.fullName,
                role:     params.role,
                status:   params.status,
                username: params.username,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/auth/users/${encodeURIComponent(params.id)}`, {method: "PUT", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_auth_users_updateUser>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { bulkDeleteTickets as api_ticket_bulk_delete_bulkDeleteTickets } from "~backend/ticket/bulk-delete";
import {
    bulkImportTickets as api_ticket_bulk_import_bulkImportTickets,
    generateImportTemplate as api_ticket_bulk_import_generateImportTemplate
} from "~backend/ticket/bulk-import";
import { closeTicket as api_ticket_close_closeTicket } from "~backend/ticket/close";
import {
    addComment as api_ticket_comments_addComment,
    deleteComment as api_ticket_comments_deleteComment,
    listComments as api_ticket_comments_listComments,
    updateComment as api_ticket_comments_updateComment
} from "~backend/ticket/comments";
import { create as api_ticket_create_create } from "~backend/ticket/create";
import { deleteTicket as api_ticket_delete_deleteTicket } from "~backend/ticket/delete";
import {
    clearOldEmailLogs as api_ticket_email_logs_clearOldEmailLogs,
    getEmailStats as api_ticket_email_logs_getEmailStats,
    listEmailLogs as api_ticket_email_logs_listEmailLogs
} from "~backend/ticket/email-logs";
import { listEngineers as api_ticket_engineers_listEngineers } from "~backend/ticket/engineers";
import { exportTickets as api_ticket_export_exportTickets } from "~backend/ticket/export";
import { get as api_ticket_get_get } from "~backend/ticket/get";
import { list as api_ticket_list_list } from "~backend/ticket/list";
import {
    configureSMTP as api_ticket_smtp_configureSMTP,
    getSMTPConfig as api_ticket_smtp_getSMTPConfig,
    testSMTP as api_ticket_smtp_testSMTP
} from "~backend/ticket/smtp";
import { getStats as api_ticket_stats_getStats } from "~backend/ticket/stats";
import {
    getSystemConfig as api_ticket_system_config_getSystemConfig,
    updateSystemConfig as api_ticket_system_config_updateSystemConfig
} from "~backend/ticket/system-config";
import { update as api_ticket_update_update } from "~backend/ticket/update";

export namespace ticket {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.addComment = this.addComment.bind(this)
            this.bulkDeleteTickets = this.bulkDeleteTickets.bind(this)
            this.bulkImportTickets = this.bulkImportTickets.bind(this)
            this.clearOldEmailLogs = this.clearOldEmailLogs.bind(this)
            this.closeTicket = this.closeTicket.bind(this)
            this.configureSMTP = this.configureSMTP.bind(this)
            this.create = this.create.bind(this)
            this.deleteComment = this.deleteComment.bind(this)
            this.deleteTicket = this.deleteTicket.bind(this)
            this.exportTickets = this.exportTickets.bind(this)
            this.generateImportTemplate = this.generateImportTemplate.bind(this)
            this.get = this.get.bind(this)
            this.getEmailStats = this.getEmailStats.bind(this)
            this.getSMTPConfig = this.getSMTPConfig.bind(this)
            this.getStats = this.getStats.bind(this)
            this.getSystemConfig = this.getSystemConfig.bind(this)
            this.list = this.list.bind(this)
            this.listComments = this.listComments.bind(this)
            this.listEmailLogs = this.listEmailLogs.bind(this)
            this.listEngineers = this.listEngineers.bind(this)
            this.testSMTP = this.testSMTP.bind(this)
            this.update = this.update.bind(this)
            this.updateComment = this.updateComment.bind(this)
            this.updateSystemConfig = this.updateSystemConfig.bind(this)
        }

        /**
         * Adds a comment to a ticket.
         */
        public async addComment(params: RequestType<typeof api_ticket_comments_addComment>): Promise<ResponseType<typeof api_ticket_comments_addComment>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                content:    params.content,
                isInternal: params.isInternal,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/tickets/${encodeURIComponent(params.ticketId)}/comments`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_ticket_comments_addComment>
        }

        /**
         * Deletes multiple tickets (admin only).
         */
        public async bulkDeleteTickets(params: RequestType<typeof api_ticket_bulk_delete_bulkDeleteTickets>): Promise<ResponseType<typeof api_ticket_bulk_delete_bulkDeleteTickets>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                ticketIds: params.ticketIds.map((v) => String(v)),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/tickets/bulk`, {query, method: "DELETE", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_ticket_bulk_delete_bulkDeleteTickets>
        }

        /**
         * Bulk import tickets from Excel file.
         */
        public async bulkImportTickets(params: RequestType<typeof api_ticket_bulk_import_bulkImportTickets>): Promise<ResponseType<typeof api_ticket_bulk_import_bulkImportTickets>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/tickets/bulk-import`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_ticket_bulk_import_bulkImportTickets>
        }

        /**
         * Clears old email logs (admin only).
         */
        public async clearOldEmailLogs(params: RequestType<typeof api_ticket_email_logs_clearOldEmailLogs>): Promise<ResponseType<typeof api_ticket_email_logs_clearOldEmailLogs>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                daysOld: String(params.daysOld),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/email-logs/cleanup`, {query, method: "DELETE", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_ticket_email_logs_clearOldEmailLogs>
        }

        /**
         * Closes a ticket and sets the resolution.
         */
        public async closeTicket(params: RequestType<typeof api_ticket_close_closeTicket>): Promise<ResponseType<typeof api_ticket_close_closeTicket>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                resolution: params.resolution,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/tickets/${encodeURIComponent(params.id)}/close`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_ticket_close_closeTicket>
        }

        /**
         * Configures SMTP settings for email notifications.
         */
        public async configureSMTP(params: RequestType<typeof api_ticket_smtp_configureSMTP>): Promise<ResponseType<typeof api_ticket_smtp_configureSMTP>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/smtp/configure`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_ticket_smtp_configureSMTP>
        }

        /**
         * Creates a new ticket.
         */
        public async create(params: RequestType<typeof api_ticket_create_create>): Promise<ResponseType<typeof api_ticket_create_create>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/tickets`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_ticket_create_create>
        }

        /**
         * Deletes a comment (only by the author or admin).
         */
        public async deleteComment(params: { id: number }): Promise<void> {
            await this.baseClient.callTypedAPI(`/comments/${encodeURIComponent(params.id)}`, {method: "DELETE", body: undefined})
        }

        /**
         * Deletes a ticket (admin only).
         */
        public async deleteTicket(params: { id: number }): Promise<void> {
            await this.baseClient.callTypedAPI(`/tickets/${encodeURIComponent(params.id)}`, {method: "DELETE", body: undefined})
        }

        /**
         * Exports tickets to Excel or PDF format.
         */
        public async exportTickets(params: RequestType<typeof api_ticket_export_exportTickets>): Promise<ResponseType<typeof api_ticket_export_exportTickets>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                assignedEngineer: params.assignedEngineer,
                endDate:          params.endDate,
                format:           String(params.format),
                priority:         params.priority === undefined ? undefined : String(params.priority),
                search:           params.search,
                startDate:        params.startDate,
                status:           params.status === undefined ? undefined : String(params.status),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/tickets/export`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_ticket_export_exportTickets>
        }

        /**
         * Generate Excel template for bulk import
         */
        public async generateImportTemplate(): Promise<ResponseType<typeof api_ticket_bulk_import_generateImportTemplate>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/tickets/import-template`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_ticket_bulk_import_generateImportTemplate>
        }

        /**
         * Retrieves a specific ticket by ID with role-based access control.
         */
        public async get(params: { id: number }): Promise<ResponseType<typeof api_ticket_get_get>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/tickets/${encodeURIComponent(params.id)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_ticket_get_get>
        }

        /**
         * Retrieves email delivery statistics (admin only).
         */
        public async getEmailStats(): Promise<ResponseType<typeof api_ticket_email_logs_getEmailStats>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/email-stats`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_ticket_email_logs_getEmailStats>
        }

        /**
         * Retrieves current SMTP configuration.
         */
        public async getSMTPConfig(): Promise<ResponseType<typeof api_ticket_smtp_getSMTPConfig>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/smtp/config`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_ticket_smtp_getSMTPConfig>
        }

        /**
         * Retrieves ticket statistics and trends with role-based filtering.
         */
        public async getStats(): Promise<ResponseType<typeof api_ticket_stats_getStats>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/tickets/stats`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_ticket_stats_getStats>
        }

        /**
         * Retrieves current system configuration (public).
         */
        public async getSystemConfig(): Promise<ResponseType<typeof api_ticket_system_config_getSystemConfig>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/system/config`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_ticket_system_config_getSystemConfig>
        }

        /**
         * Retrieves tickets based on user role and permissions.
         */
        public async list(params: RequestType<typeof api_ticket_list_list>): Promise<ResponseType<typeof api_ticket_list_list>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                assignedEngineer: params.assignedEngineer,
                endDate:          params.endDate,
                limit:            params.limit === undefined ? undefined : String(params.limit),
                offset:           params.offset === undefined ? undefined : String(params.offset),
                priority:         params.priority === undefined ? undefined : String(params.priority),
                search:           params.search,
                sortField:        params.sortField === undefined ? undefined : String(params.sortField),
                sortOrder:        params.sortOrder === undefined ? undefined : String(params.sortOrder),
                startDate:        params.startDate,
                status:           params.status === undefined ? undefined : String(params.status),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/tickets`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_ticket_list_list>
        }

        /**
         * Retrieves all comments for a ticket with role-based filtering.
         */
        public async listComments(params: { ticketId: number }): Promise<ResponseType<typeof api_ticket_comments_listComments>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/tickets/${encodeURIComponent(params.ticketId)}/comments`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_ticket_comments_listComments>
        }

        /**
         * Retrieves email delivery logs (admin only).
         */
        public async listEmailLogs(params: RequestType<typeof api_ticket_email_logs_listEmailLogs>): Promise<ResponseType<typeof api_ticket_email_logs_listEmailLogs>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:    params.limit === undefined ? undefined : String(params.limit),
                offset:   params.offset === undefined ? undefined : String(params.offset),
                status:   params.status === undefined ? undefined : String(params.status),
                ticketId: params.ticketId === undefined ? undefined : String(params.ticketId),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/email-logs`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_ticket_email_logs_listEmailLogs>
        }

        /**
         * Retrieves all engineers from the auth system.
         */
        public async listEngineers(): Promise<ResponseType<typeof api_ticket_engineers_listEngineers>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/engineers`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_ticket_engineers_listEngineers>
        }

        /**
         * Tests SMTP configuration.
         */
        public async testSMTP(params: RequestType<typeof api_ticket_smtp_testSMTP>): Promise<ResponseType<typeof api_ticket_smtp_testSMTP>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/smtp/test`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_ticket_smtp_testSMTP>
        }

        /**
         * Updates an existing ticket with role-based permissions.
         */
        public async update(params: RequestType<typeof api_ticket_update_update>): Promise<ResponseType<typeof api_ticket_update_update>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                assignedEngineer: params.assignedEngineer,
                companyName:      params.companyName,
                customDate:       params.customDate,
                description:      params.description,
                priority:         params.priority,
                reporterEmail:    params.reporterEmail,
                reporterName:     params.reporterName,
                resolution:       params.resolution,
                status:           params.status,
                subject:          params.subject,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/tickets/${encodeURIComponent(params.id)}`, {method: "PUT", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_ticket_update_update>
        }

        /**
         * Updates a comment (only by the author or admin).
         */
        public async updateComment(params: RequestType<typeof api_ticket_comments_updateComment>): Promise<ResponseType<typeof api_ticket_comments_updateComment>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                content: params.content,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/comments/${encodeURIComponent(params.id)}`, {method: "PUT", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_ticket_comments_updateComment>
        }

        /**
         * Updates system configuration (admin only).
         */
        public async updateSystemConfig(params: RequestType<typeof api_ticket_system_config_updateSystemConfig>): Promise<ResponseType<typeof api_ticket_system_config_updateSystemConfig>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/system/config`, {method: "PUT", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_ticket_system_config_updateSystemConfig>
        }
    }
}


type PickMethods<Type> = Omit<CallParameters, "method"> & { method?: Type };

// Helper type to omit all fields that are cookies.
type OmitCookie<T> = {
  [K in keyof T as T[K] extends CookieWithOptions<any> ? never : K]: T[K];
};

type RequestType<Type extends (...args: any[]) => any> =
  Parameters<Type> extends [infer H, ...any[]]
    ? OmitCookie<H>
    : void;

type ResponseType<Type extends (...args: any[]) => any> = OmitCookie<Awaited<ReturnType<Type>>>;

function dateReviver(key: string, value: any): any {
  if (
    typeof value === "string" &&
    value.length >= 10 &&
    value.charCodeAt(0) >= 48 && // '0'
    value.charCodeAt(0) <= 57 // '9'
  ) {
    const parsedDate = new Date(value);
    if (!isNaN(parsedDate.getTime())) {
      return parsedDate;
    }
  }
  return value;
}


function encodeQuery(parts: Record<string, string | string[]>): string {
    const pairs: string[] = []
    for (const key in parts) {
        const val = (Array.isArray(parts[key]) ?  parts[key] : [parts[key]]) as string[]
        for (const v of val) {
            pairs.push(`${key}=${encodeURIComponent(v)}`)
        }
    }
    return pairs.join("&")
}

// makeRecord takes a record and strips any undefined values from it,
// and returns the same record with a narrower type.
// @ts-ignore - TS ignore because makeRecord is not always used
function makeRecord<K extends string | number | symbol, V>(record: Record<K, V | undefined>): Record<K, V> {
    for (const key in record) {
        if (record[key] === undefined) {
            delete record[key]
        }
    }
    return record as Record<K, V>
}

import {
  StreamInOutHandlerFn,
  StreamInHandlerFn,
  StreamOutHandlerFn,
} from "encore.dev/api";

type StreamRequest<Type> = Type extends
  | StreamInOutHandlerFn<any, infer Req, any>
  | StreamInHandlerFn<any, infer Req, any>
  | StreamOutHandlerFn<any, any>
  ? Req
  : never;

type StreamResponse<Type> = Type extends
  | StreamInOutHandlerFn<any, any, infer Resp>
  | StreamInHandlerFn<any, any, infer Resp>
  | StreamOutHandlerFn<any, infer Resp>
  ? Resp
  : never;


function encodeWebSocketHeaders(headers: Record<string, string>) {
    // url safe, no pad
    const base64encoded = btoa(JSON.stringify(headers))
      .replaceAll("=", "")
      .replaceAll("+", "-")
      .replaceAll("/", "_");
    return "encore.dev.headers." + base64encoded;
}

class WebSocketConnection {
    public ws: WebSocket;

    private hasUpdateHandlers: (() => void)[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        let protocols = ["encore-ws"];
        if (headers) {
            protocols.push(encodeWebSocketHeaders(headers))
        }

        this.ws = new WebSocket(url, protocols)

        this.on("error", () => {
            this.resolveHasUpdateHandlers();
        });

        this.on("close", () => {
            this.resolveHasUpdateHandlers();
        });
    }

    resolveHasUpdateHandlers() {
        const handlers = this.hasUpdateHandlers;
        this.hasUpdateHandlers = [];

        for (const handler of handlers) {
            handler()
        }
    }

    async hasUpdate() {
        // await until a new message have been received, or the socket is closed
        await new Promise((resolve) => {
            this.hasUpdateHandlers.push(() => resolve(null))
        });
    }

    on(type: "error" | "close" | "message" | "open", handler: (event: any) => void) {
        this.ws.addEventListener(type, handler);
    }

    off(type: "error" | "close" | "message" | "open", handler: (event: any) => void) {
        this.ws.removeEventListener(type, handler);
    }

    close() {
        this.ws.close();
    }
}

export class StreamInOut<Request, Response> {
    public socket: WebSocketConnection;
    private buffer: Response[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            this.buffer.push(JSON.parse(event.data, dateReviver));
            this.socket.resolveHasUpdateHandlers();
        });
    }

    close() {
        this.socket.close();
    }

    async send(msg: Request) {
        if (this.socket.ws.readyState === WebSocket.CONNECTING) {
            // await that the socket is opened
            await new Promise((resolve) => {
                this.socket.ws.addEventListener("open", resolve, { once: true });
            });
        }

        return this.socket.ws.send(JSON.stringify(msg));
    }

    async next(): Promise<Response | undefined> {
        for await (const next of this) return next;
        return undefined;
    }

    async *[Symbol.asyncIterator](): AsyncGenerator<Response, undefined, void> {
        while (true) {
            if (this.buffer.length > 0) {
                yield this.buffer.shift() as Response;
            } else {
                if (this.socket.ws.readyState === WebSocket.CLOSED) return;
                await this.socket.hasUpdate();
            }
        }
    }
}

export class StreamIn<Response> {
    public socket: WebSocketConnection;
    private buffer: Response[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            this.buffer.push(JSON.parse(event.data, dateReviver));
            this.socket.resolveHasUpdateHandlers();
        });
    }

    close() {
        this.socket.close();
    }

    async next(): Promise<Response | undefined> {
        for await (const next of this) return next;
        return undefined;
    }

    async *[Symbol.asyncIterator](): AsyncGenerator<Response, undefined, void> {
        while (true) {
            if (this.buffer.length > 0) {
                yield this.buffer.shift() as Response;
            } else {
                if (this.socket.ws.readyState === WebSocket.CLOSED) return;
                await this.socket.hasUpdate();
            }
        }
    }
}

export class StreamOut<Request, Response> {
    public socket: WebSocketConnection;
    private responseValue: Promise<Response>;

    constructor(url: string, headers?: Record<string, string>) {
        let responseResolver: (_: any) => void;
        this.responseValue = new Promise((resolve) => responseResolver = resolve);

        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            responseResolver(JSON.parse(event.data, dateReviver))
        });
    }

    async response(): Promise<Response> {
        return this.responseValue;
    }

    close() {
        this.socket.close();
    }

    async send(msg: Request) {
        if (this.socket.ws.readyState === WebSocket.CONNECTING) {
            // await that the socket is opened
            await new Promise((resolve) => {
                this.socket.ws.addEventListener("open", resolve, { once: true });
            });
        }

        return this.socket.ws.send(JSON.stringify(msg));
    }
}
// CallParameters is the type of the parameters to a method call, but require headers to be a Record type
type CallParameters = Omit<RequestInit, "headers"> & {
    /** Headers to be sent with the request */
    headers?: Record<string, string>

    /** Query parameters to be sent with the request */
    query?: Record<string, string | string[]>
}

// AuthDataGenerator is a function that returns a new instance of the authentication data required by this API
export type AuthDataGenerator = () =>
  | RequestType<typeof auth_auth>
  | Promise<RequestType<typeof auth_auth> | undefined>
  | undefined;

// A fetcher is the prototype for the inbuilt Fetch function
export type Fetcher = typeof fetch;

const boundFetch = fetch.bind(this);

class BaseClient {
    readonly baseURL: string
    readonly fetcher: Fetcher
    readonly headers: Record<string, string>
    readonly requestInit: Omit<RequestInit, "headers"> & { headers?: Record<string, string> }
    readonly authGenerator?: AuthDataGenerator

    constructor(baseURL: string, options: ClientOptions) {
        this.baseURL = baseURL
        this.headers = {}

        // Add User-Agent header if the script is running in the server
        // because browsers do not allow setting User-Agent headers to requests
        if (!BROWSER) {
            this.headers["User-Agent"] = "-Generated-TS-Client (Encore/1.50.4)";
        }

        this.requestInit = options.requestInit ?? {};

        // Setup what fetch function we'll be using in the base client
        if (options.fetcher !== undefined) {
            this.fetcher = options.fetcher
        } else {
            this.fetcher = boundFetch
        }

        // Setup an authentication data generator using the auth data token option
        if (options.auth !== undefined) {
            const auth = options.auth
            if (typeof auth === "function") {
                this.authGenerator = auth
            } else {
                this.authGenerator = () => auth
            }
        }
    }

    async getAuthData(): Promise<CallParameters | undefined> {
        let authData: RequestType<typeof auth_auth> | undefined;

        // If authorization data generator is present, call it and add the returned data to the request
        if (this.authGenerator) {
            const mayBePromise = this.authGenerator();
            if (mayBePromise instanceof Promise) {
                authData = await mayBePromise;
            } else {
                authData = mayBePromise;
            }
        }

        if (authData) {
            const data: CallParameters = {};

            data.headers = makeRecord<string, string>({
                authorization: authData.authorization,
            });

            return data;
        }

        return undefined;
    }

    // createStreamInOut sets up a stream to a streaming API endpoint.
    async createStreamInOut<Request, Response>(path: string, params?: CallParameters): Promise<StreamInOut<Request, Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamInOut(this.baseURL + path + queryString, headers);
    }

    // createStreamIn sets up a stream to a streaming API endpoint.
    async createStreamIn<Response>(path: string, params?: CallParameters): Promise<StreamIn<Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamIn(this.baseURL + path + queryString, headers);
    }

    // createStreamOut sets up a stream to a streaming API endpoint.
    async createStreamOut<Request, Response>(path: string, params?: CallParameters): Promise<StreamOut<Request, Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamOut(this.baseURL + path + queryString, headers);
    }

    // callTypedAPI makes an API call, defaulting content type to "application/json"
    public async callTypedAPI(path: string, params?: CallParameters): Promise<Response> {
        return this.callAPI(path, {
            ...params,
            headers: { "Content-Type": "application/json", ...params?.headers }
        });
    }

    // callAPI is used by each generated API method to actually make the request
    public async callAPI(path: string, params?: CallParameters): Promise<Response> {
        let { query, headers, ...rest } = params ?? {}
        const init = {
            ...this.requestInit,
            ...rest,
        }

        // Merge our headers with any predefined headers
        init.headers = {...this.headers, ...init.headers, ...headers}

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                init.headers = {...init.headers, ...authData.headers};
            }
        }

        // Make the actual request
        const queryString = query ? '?' + encodeQuery(query) : ''
        const response = await this.fetcher(this.baseURL+path+queryString, init)

        // handle any error responses
        if (!response.ok) {
            // try and get the error message from the response body
            let body: APIErrorResponse = { code: ErrCode.Unknown, message: `request failed: status ${response.status}` }

            // if we can get the structured error we should, otherwise give a best effort
            try {
                const text = await response.text()

                try {
                    const jsonBody = JSON.parse(text)
                    if (isAPIErrorResponse(jsonBody)) {
                        body = jsonBody
                    } else {
                        body.message += ": " + JSON.stringify(jsonBody)
                    }
                } catch {
                    body.message += ": " + text
                }
            } catch (e) {
                // otherwise we just append the text to the error message
                body.message += ": " + String(e)
            }

            throw new APIError(response.status, body)
        }

        return response
    }
}

/**
 * APIErrorDetails represents the response from an Encore API in the case of an error
 */
interface APIErrorResponse {
    code: ErrCode
    message: string
    details?: any
}

function isAPIErrorResponse(err: any): err is APIErrorResponse {
    return (
        err !== undefined && err !== null &&
        isErrCode(err.code) &&
        typeof(err.message) === "string" &&
        (err.details === undefined || err.details === null || typeof(err.details) === "object")
    )
}

function isErrCode(code: any): code is ErrCode {
    return code !== undefined && Object.values(ErrCode).includes(code)
}

/**
 * APIError represents a structured error as returned from an Encore application.
 */
export class APIError extends Error {
    /**
     * The HTTP status code associated with the error.
     */
    public readonly status: number

    /**
     * The Encore error code
     */
    public readonly code: ErrCode

    /**
     * The error details
     */
    public readonly details?: any

    constructor(status: number, response: APIErrorResponse) {
        // extending errors causes issues after you construct them, unless you apply the following fixes
        super(response.message);

        // set error name as constructor name, make it not enumerable to keep native Error behavior
        // https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/new.target#new.target_in_constructors
        Object.defineProperty(this, 'name', {
            value:        'APIError',
            enumerable:   false,
            configurable: true,
        })

        // fix the prototype chain
        if ((Object as any).setPrototypeOf == undefined) {
            (this as any).__proto__ = APIError.prototype
        } else {
            Object.setPrototypeOf(this, APIError.prototype);
        }

        // capture a stack trace
        if ((Error as any).captureStackTrace !== undefined) {
            (Error as any).captureStackTrace(this, this.constructor);
        }

        this.status = status
        this.code = response.code
        this.details = response.details
    }
}

/**
 * Typeguard allowing use of an APIError's fields'
 */
export function isAPIError(err: any): err is APIError {
    return err instanceof APIError;
}

export enum ErrCode {
    /**
     * OK indicates the operation was successful.
     */
    OK = "ok",

    /**
     * Canceled indicates the operation was canceled (typically by the caller).
     *
     * Encore will generate this error code when cancellation is requested.
     */
    Canceled = "canceled",

    /**
     * Unknown error. An example of where this error may be returned is
     * if a Status value received from another address space belongs to
     * an error-space that is not known in this address space. Also
     * errors raised by APIs that do not return enough error information
     * may be converted to this error.
     *
     * Encore will generate this error code in the above two mentioned cases.
     */
    Unknown = "unknown",

    /**
     * InvalidArgument indicates client specified an invalid argument.
     * Note that this differs from FailedPrecondition. It indicates arguments
     * that are problematic regardless of the state of the system
     * (e.g., a malformed file name).
     *
     * This error code will not be generated by the gRPC framework.
     */
    InvalidArgument = "invalid_argument",

    /**
     * DeadlineExceeded means operation expired before completion.
     * For operations that change the state of the system, this error may be
     * returned even if the operation has completed successfully. For
     * example, a successful response from a server could have been delayed
     * long enough for the deadline to expire.
     *
     * The gRPC framework will generate this error code when the deadline is
     * exceeded.
     */
    DeadlineExceeded = "deadline_exceeded",

    /**
     * NotFound means some requested entity (e.g., file or directory) was
     * not found.
     *
     * This error code will not be generated by the gRPC framework.
     */
    NotFound = "not_found",

    /**
     * AlreadyExists means an attempt to create an entity failed because one
     * already exists.
     *
     * This error code will not be generated by the gRPC framework.
     */
    AlreadyExists = "already_exists",

    /**
     * PermissionDenied indicates the caller does not have permission to
     * execute the specified operation. It must not be used for rejections
     * caused by exhausting some resource (use ResourceExhausted
     * instead for those errors). It must not be
     * used if the caller cannot be identified (use Unauthenticated
     * instead for those errors).
     *
     * This error code will not be generated by the gRPC core framework,
     * but expect authentication middleware to use it.
     */
    PermissionDenied = "permission_denied",

    /**
     * ResourceExhausted indicates some resource has been exhausted, perhaps
     * a per-user quota, or perhaps the entire file system is out of space.
     *
     * This error code will be generated by the gRPC framework in
     * out-of-memory and server overload situations, or when a message is
     * larger than the configured maximum size.
     */
    ResourceExhausted = "resource_exhausted",

    /**
     * FailedPrecondition indicates operation was rejected because the
     * system is not in a state required for the operation's execution.
     * For example, directory to be deleted may be non-empty, an rmdir
     * operation is applied to a non-directory, etc.
     *
     * A litmus test that may help a service implementor in deciding
     * between FailedPrecondition, Aborted, and Unavailable:
     *  (a) Use Unavailable if the client can retry just the failing call.
     *  (b) Use Aborted if the client should retry at a higher-level
     *      (e.g., restarting a read-modify-write sequence).
     *  (c) Use FailedPrecondition if the client should not retry until
     *      the system state has been explicitly fixed. E.g., if an "rmdir"
     *      fails because the directory is non-empty, FailedPrecondition
     *      should be returned since the client should not retry unless
     *      they have first fixed up the directory by deleting files from it.
     *  (d) Use FailedPrecondition if the client performs conditional
     *      REST Get/Update/Delete on a resource and the resource on the
     *      server does not match the condition. E.g., conflicting
     *      read-modify-write on the same resource.
     *
     * This error code will not be generated by the gRPC framework.
     */
    FailedPrecondition = "failed_precondition",

    /**
     * Aborted indicates the operation was aborted, typically due to a
     * concurrency issue like sequencer check failures, transaction aborts,
     * etc.
     *
     * See litmus test above for deciding between FailedPrecondition,
     * Aborted, and Unavailable.
     */
    Aborted = "aborted",

    /**
     * OutOfRange means operation was attempted past the valid range.
     * E.g., seeking or reading past end of file.
     *
     * Unlike InvalidArgument, this error indicates a problem that may
     * be fixed if the system state changes. For example, a 32-bit file
     * system will generate InvalidArgument if asked to read at an
     * offset that is not in the range [0,2^32-1], but it will generate
     * OutOfRange if asked to read from an offset past the current
     * file size.
     *
     * There is a fair bit of overlap between FailedPrecondition and
     * OutOfRange. We recommend using OutOfRange (the more specific
     * error) when it applies so that callers who are iterating through
     * a space can easily look for an OutOfRange error to detect when
     * they are done.
     *
     * This error code will not be generated by the gRPC framework.
     */
    OutOfRange = "out_of_range",

    /**
     * Unimplemented indicates operation is not implemented or not
     * supported/enabled in this service.
     *
     * This error code will be generated by the gRPC framework. Most
     * commonly, you will see this error code when a method implementation
     * is missing on the server. It can also be generated for unknown
     * compression algorithms or a disagreement as to whether an RPC should
     * be streaming.
     */
    Unimplemented = "unimplemented",

    /**
     * Internal errors. Means some invariants expected by underlying
     * system has been broken. If you see one of these errors,
     * something is very broken.
     *
     * This error code will be generated by the gRPC framework in several
     * internal error conditions.
     */
    Internal = "internal",

    /**
     * Unavailable indicates the service is currently unavailable.
     * This is a most likely a transient condition and may be corrected
     * by retrying with a backoff. Note that it is not always safe to retry
     * non-idempotent operations.
     *
     * See litmus test above for deciding between FailedPrecondition,
     * Aborted, and Unavailable.
     *
     * This error code will be generated by the gRPC framework during
     * abrupt shutdown of a server process or network connection.
     */
    Unavailable = "unavailable",

    /**
     * DataLoss indicates unrecoverable data loss or corruption.
     *
     * This error code will not be generated by the gRPC framework.
     */
    DataLoss = "data_loss",

    /**
     * Unauthenticated indicates the request does not have valid
     * authentication credentials for the operation.
     *
     * The gRPC framework will generate this error code when the
     * authentication metadata is invalid or a Credentials callback fails,
     * but also expect authentication middleware to generate it.
     */
    Unauthenticated = "unauthenticated",
}

export default new Client(import.meta.env.VITE_CLIENT_TARGET, { requestInit: { credentials: "include" } });
